    // Undefined. // Undefined is absolutely equal to itself.
    // Undeclared.  
    
//  Undefined Means There's definitely a variable but at the moment we didn't initialize it with any value but there is one values implicitly exists on that type which is undefined..
//  Undeclared Means It's never been declared in any scope. That we access to.